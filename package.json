{
  "name": "alistamento-bot",
  "version": "1.1.0",
  "description": "Bot para acessar alistamento militar EB e capturar certificado",
  "main": "src/bot.js",
  "scripts": {
    "start": "node src/bot.js",
    "save-cred": "node -e \"const {CredentialsManager} = require('./src/credentials'); new CredentialsManager().save(process.argv[2], process.argv[3]);\"",
    "run-multiple": "node run-multiple.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "dotenv": "^17.3.1",
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
